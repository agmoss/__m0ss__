{
  "name": "root",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "bootstrap:clean": "lerna clean -y && lerna bootstrap --use-workspaces",
    "lint": "lerna run lint --stream --parallel  --scope={m0ss,three-ui}",
    "format": "lerna run format --stream --parallel  --scope={m0ss,three-ui}"
  },
  "engines": {
    "node": "12.X",
    "yarn": ">=1.19.1"
  }
}
